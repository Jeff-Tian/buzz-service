export const Messages = {
    ClassHourChanged: 'class-hour-changed',
}

export default class MessageBus {
    static emitMessage(message, payload) {

    }

    static handleMessage(message, handler) {

    }
}
